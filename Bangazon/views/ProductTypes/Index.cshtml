@model IEnumerable<Bangazon.Models.ProductTypesViewModels.ProductTypesViewModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                Model.GroupedProducts
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @*<td>
                @item.GroupedProducts.TypeName
            </td>
            <td>
                @item.GroupedProducts.ProductCount
            </td>*@
            <td>
            <a asp-action="" asp-route-id="@item.GroupedProducts">@item.GroupedProducts</a> |
            @*<a asp-action="Details" asp-route-id="@item.ProductTypeId">Details</a> |*@
            @*<a asp-action="Delete" asp-route-id="@item.ProductTypeId">Delete</a>*@
        </td>
        </tr>
}
    </tbody>
</table>
